[pytest]
# pythonpath = . ensures both the repo's own `app` package and any modules
# DevPilot injects at the workspace root (e.g. `devpilot_changes/`) are importable.
pythonpath = .
testpaths = tests
addopts = -q
