# Sample Node Repo (DevPilot demo target)

A tiny in-memory order service used to demonstrate DevPilot AI's project-type
detection (Node/`npm test`) and multi-language ingestion.

```bash
npm test
```
