# ---- Core (required for mock mode, zero external services) ----
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
sqlalchemy==2.0.36
numpy==2.1.3
python-multipart==0.0.17
httpx==0.28.1

# ---- Observability ----
prometheus-client==0.21.1

# ---- Agents (graph runtime) ----
langgraph==0.2.53

# ---- Optional: real local Postgres + pgvector (docker-compose path) ----
psycopg[binary]==3.2.3
pgvector==0.3.6

# ---- Optional: real semantic embeddings (falls back to deterministic hash embeddings if absent) ----
# sentence-transformers==3.3.1
# torch  # pulled in by sentence-transformers

# ---- Optional: Redis cache / queue ----
redis==5.2.0

# ---- Optional: AST-aware chunking (falls back to line-window chunking if absent) ----
# tree-sitter==0.23.2
# tree-sitter-languages==1.10.2

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.24.0
